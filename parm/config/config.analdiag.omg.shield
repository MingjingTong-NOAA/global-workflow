#! /usr/bin/env bash

########## config.analdiag ##########
# GFS post-anal specific (diag)

echo "BEGIN: config.analdiag"

# Get task specific resources
. $EXPDIR/config.resources analdiag

export pcp_jacobian=".true."

export GDUMP=$CDUMP
export GENDIAG="YES"
export DIAG_TARBALL="YES"

export loops="01"

export RADSTAT=${COMOUTatmos}/${CDUMP}.t${cyc}z.radstat.omf

echo "END: config.analdiag"
