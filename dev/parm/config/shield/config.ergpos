#! /usr/bin/env bash

########## config.ergpos ##########
# Compute ensemble mean and spread on regrided grid 

echo "BEGIN: config.ergpos"

# Get task specific resources
source "$EXPDIR/config.resources" ergpos

export grids="1p00 0p50 0p25"

echo "END: config.ergpos"
