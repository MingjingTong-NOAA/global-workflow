#! /usr/bin/env bash
# shellcheck disable=SC2034

# GaeaC5-specific job resources

export FI_VERBS_PREFER_XRC=0

unset memory
unset "memory_${RUN}"

case ${step} in
  "init")
    tasks_per_node=24
  ;;
  *)
  ;;
esac
